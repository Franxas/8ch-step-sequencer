-> function to shape the driving phasor.  
-> randomize steps of the sequence.  
-> each channel its own tempo and trigger probability. velocity, ratcheting and modulation for each step.  
-> matrix for ducking on trigger events.  

-> each channel ouputs a list of three values (midi note, velocity 0 to 127, modulation 0 to 1).
